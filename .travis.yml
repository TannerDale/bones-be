language: ruby
rbenv:
- 2.7.2
addons:
  postgresql: 13
script:
- rails db:{create,migrate} RAILS_ENV=test
- bundle exec rspec
deploy:
  provider:
  api_key:
    secure:
  app:
  run:
  on:
    branch: main
